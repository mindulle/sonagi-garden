---
import BaseLayout from '@/layouts/Base.astro';
import { Button, Card } from '@/components/ui';
---

<BaseLayout title="홈">
  <div class="hero">
    <div class="hero-content">
      <div class="hero-icon">🌱</div>
      <h1 class="hero-title">Sonagi Garden</h1>
      <p class="hero-subtitle">
        청량한 디지털 정원에 오신 것을 환영합니다
      </p>
      <p class="hero-description">
        비 온 뒤의 맑고 깨끗한 느낌처럼,<br>
        정보를 가꾸고 연결하는 공간입니다.
      </p>
      
      <div class="hero-actions">
        <a href="/notes/dev">
          <Button variant="primary" size="lg">노트 탐색하기</Button>
        </a>
        <a href="#about">
          <Button variant="secondary" size="lg">더 알아보기</Button>
        </a>
      </div>
    </div>
  </div>
  
  <section class="features" id="about">
    <div class="container">
      <h2 class="section-title">디지털 정원이란?</h2>
      
      <div class="feature-grid">
        <Card className="feature-card">
          <div class="feature-icon">🌿</div>
          <h3>유기적 성장</h3>
          <p>노트는 시간이 지나며 자라고 연결됩니다. 완벽하지 않아도 괜찮아요.</p>
        </Card>
        
        <Card className="feature-card">
          <div class="feature-icon">🔗</div>
          <h3>자유로운 연결</h3>
          <p>아이디어와 개념을 자유롭게 연결하여 새로운 통찰을 만들어냅니다.</p>
        </Card>
        
        <Card className="feature-card">
          <div class="feature-icon">💧</div>
          <h3>청량한 경험</h3>
          <p>Sonagi 디자인 시스템으로 깔끔하고 시원한 독서 경험을 제공합니다.</p>
        </Card>
        
        <Card className="feature-card">
          <div class="feature-icon">📝</div>
          <h3>마크다운 기반</h3>
          <p>Obsidian으로 작성한 노트를 그대로 웹에 배포할 수 있습니다.</p>
        </Card>
      </div>
    </div>
  </section>
  
  <section class="domains">
    <div class="container">
      <h2 class="section-title">탐색하기</h2>
      
      <div class="domain-grid">
        <a href="/notes/dev" class="domain-link">
          <Card variant="bordered" className="domain-card">
            <div class="domain-icon">💻</div>
            <h3>Dev</h3>
            <p>개발, 프로그래밍, 기술 관련 노트</p>
          </Card>
        </a>
        
        <a href="/notes/design" class="domain-link">
          <Card variant="bordered" className="domain-card">
            <div class="domain-icon">🎨</div>
            <h3>Design</h3>
            <p>디자인, UX/UI, 크리에이티브 관련 노트</p>
          </Card>
        </a>
        
        <a href="/notes/life" class="domain-link">
          <Card variant="bordered" className="domain-card">
            <div class="domain-icon">✨</div>
            <h3>Life</h3>
            <p>일상, 생각, 에세이</p>
          </Card>
        </a>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  /* Hero Section */
  .hero {
    padding: var(--sonagi-spacing-2xl) 0;
    text-align: center;
  }
  
  .hero-content {
    max-width: 700px;
    margin: 0 auto;
    padding: 0 var(--sonagi-spacing-lg);
  }
  
  .hero-icon {
    font-size: 4rem;
    margin-bottom: var(--sonagi-spacing-lg);
    animation: float 3s ease-in-out infinite;
  }
  
  @keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
  }
  
  .hero-title {
    font-size: var(--sonagi-font-size-3xl);
    margin-bottom: var(--sonagi-spacing-md);
    color: var(--sonagi-color-primary);
  }
  
  .hero-subtitle {
    font-size: var(--sonagi-font-size-xl);
    color: var(--sonagi-color-text-secondary);
    margin-bottom: var(--sonagi-spacing-lg);
  }
  
  .hero-description {
    font-size: var(--sonagi-font-size-base);
    color: var(--sonagi-color-text-secondary);
    line-height: 1.8;
    margin-bottom: var(--sonagi-spacing-2xl);
  }
  
  .hero-actions {
    display: flex;
    gap: var(--sonagi-spacing-md);
    justify-content: center;
    flex-wrap: wrap;
  }
  
  /* Sections */
  .features, .domains {
    padding: var(--sonagi-spacing-2xl) 0;
  }
  
  .section-title {
    text-align: center;
    font-size: var(--sonagi-font-size-2xl);
    margin-bottom: var(--sonagi-spacing-2xl);
  }
  
  /* Feature Grid */
  .feature-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: var(--sonagi-spacing-xl);
    max-width: var(--max-width);
    margin: 0 auto;
  }
  
  .feature-card {
    text-align: center;
    height: 100%;
  }
  
  .feature-icon {
    font-size: 2.5rem;
    margin-bottom: var(--sonagi-spacing-md);
  }
  
  .feature-card h3 {
    margin-bottom: var(--sonagi-spacing-sm);
    color: var(--sonagi-color-text-primary);
  }
  
  .feature-card p {
    color: var(--sonagi-color-text-secondary);
    font-size: var(--sonagi-font-size-sm);
    margin: 0;
  }
  
  /* Domain Grid */
  .domain-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: var(--sonagi-spacing-xl);
    max-width: var(--max-width);
    margin: 0 auto;
  }
  
  .domain-link {
    text-decoration: none;
    display: block;
  }
  
  .domain-card {
    text-align: center;
    height: 100%;
    transition: all var(--sonagi-transition-base);
  }
  
  .domain-card:hover {
    border-color: var(--sonagi-color-primary) !important;
    transform: translateY(-4px);
  }
  
  .domain-icon {
    font-size: 3rem;
    margin-bottom: var(--sonagi-spacing-lg);
  }
  
  .domain-card h3 {
    font-size: var(--sonagi-font-size-xl);
    margin-bottom: var(--sonagi-spacing-sm);
    color: var(--sonagi-color-primary);
  }
  
  .domain-card p {
    color: var(--sonagi-color-text-secondary);
    margin: 0;
  }
  
  @media (max-width: 768px) {
    .hero-title {
      font-size: var(--sonagi-font-size-2xl);
    }
    
    .hero-subtitle {
      font-size: var(--sonagi-font-size-lg);
    }
    
    .feature-grid,
    .domain-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
