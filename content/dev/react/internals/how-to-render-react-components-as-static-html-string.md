---
title: "How to render React components as static HTML string"
date: 2025-10-28
tags: [react, internals, ssr, rendering, dom, component, html]
category: dev
description: "The `renderToString` function in React is part of the `react-dom/server` package and is used to render React components on the server-side to a static..."
---

The `renderToString` function in React is part of the `react-dom/server` package and is used to render React components on the server-side to a static HTML string. It is commonly used for server-side rendering (SSR) in React.